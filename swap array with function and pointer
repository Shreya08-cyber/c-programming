#include <stdio.h>
void swapElements(int *a,int *b){ int temp=*a; *a=*b; *b=temp; }
int main(){
    int arr[]={10,20,30};
    swapElements(&arr[0],&arr[2]);
    for(int i=0;i<3;i++) printf("%d ",arr[i]);
    return 0;
}
